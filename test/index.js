'use strict'
/*global describe, it, before, after*/

const config = require('config')
const assert = require('assert')
const request = require('supertest')

const app = require('../app')

describe('loghub-core', function () {

})
